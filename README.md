
Gemini
Nova conversa
Pesquisar conversas
Imagens
Novo
Biblioteca
Novo notebook
Data Transmission: Sampling and Quantization Exercises
Oferta e Demanda: Mercado e Comportamento do Consumidor
Todos os notebooks
Projeto de Telemetria de Foguete com ESP32
Resolução Exercício Transmissão de Dados
Frases de Inglês com Futuro Simples
Tabela de Compras com Checkbox
Exercícios de Transformada de Laplace Resolvidos
Ideias de Aulas Interativas de Inglês
Lab de Transmissão de Dados em MATLAB
Forza Horizon 6 Crash FHE01
Projetos de Análise de Dados com Pandas
Fone Bluetooth com Áudio Abafado no PC
Guia de Estudos para Estágio em Risco
Kit Volante, Pedais e Câmbio PC
Descobrir Senha Wi-Fi Conectado
Luz Amarela do Motor: O Que Fazer
Tutorial Limpeza Banheiro com Produtos
Transferir Conversas WhatsApp Celular Antigo Novo
Termo de Desligamento de Voluntário
Como saber sua memória RAM
Chaves para Montar Monitor
ESP32 DevKit V1 Pinout e Uso
Contabilidade e Regulação Bancária para Iniciantes
Descobrir Senha Wi-Fi Salva no Celular
Refatoração de Entidades JPA com Herança
Erro "Missing cloud file system scheme"
Guia Rápido: Fogão de Indução Electrolux
Aprendizado Guiado: Contabilidade e Regulação Bancária
Roteiro de Transmissão de Dados MATLAB
Convênio SESC: Benefício Completo em SP
QUERO ADICIONAR MAIS 10 LINYHAS NESTA PLANILHA SEGUINDO O MODELO JA EXISTENTE DELAS E SEM QUEBRAR A FORMULA, COMO FAZER?
Erro no Portal de Estágios UTFPR
Ideias de Atividades Interativas para Inglês
Extração de Dados para Planilha
Exercício de Antena e SNR
Conversa com o Gemini
"C:\Program Files\Java\jdk-17\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA 2025.2.3\lib\idea_rt.jar=56952" -Dfile.encoding=UTF-8 -classpath "C:\Users\Victor\Documents\STGT backend\target\classes;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-data-jpa\4.1.0\spring-boot-starter-data-jpa-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter\4.1.0\spring-boot-starter-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-logging\4.1.0\spring-boot-starter-logging-4.1.0.jar;C:\Users\Victor\.m2\repository\ch\qos\logback\logback-classic\1.5.34\logback-classic-1.5.34.jar;C:\Users\Victor\.m2\repository\ch\qos\logback\logback-core\1.5.34\logback-core-1.5.34.jar;C:\Users\Victor\.m2\repository\org\apache\logging\log4j\log4j-to-slf4j\2.25.4\log4j-to-slf4j-2.25.4.jar;C:\Users\Victor\.m2\repository\org\apache\logging\log4j\log4j-api\2.25.4\log4j-api-2.25.4.jar;C:\Users\Victor\.m2\repository\org\slf4j\jul-to-slf4j\2.0.18\jul-to-slf4j-2.0.18.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-autoconfigure\4.1.0\spring-boot-autoconfigure-4.1.0.jar;C:\Users\Victor\.m2\repository\jakarta\annotation\jakarta.annotation-api\3.0.0\jakarta.annotation-api-3.0.0.jar;C:\Users\Victor\.m2\repository\org\yaml\snakeyaml\2.6\snakeyaml-2.6.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-data-jpa\4.1.0\spring-boot-data-jpa-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-data-commons\4.1.0\spring-boot-data-commons-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-persistence\4.1.0\spring-boot-persistence-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\data\spring-data-commons\4.1.0\spring-data-commons-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-hibernate\4.1.0\spring-boot-hibernate-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-jpa\4.1.0\spring-boot-jpa-4.1.0.jar;C:\Users\Victor\.m2\repository\jakarta\persistence\jakarta.persistence-api\3.2.0\jakarta.persistence-api-3.2.0.jar;C:\Users\Victor\.m2\repository\org\hibernate\orm\hibernate-core\7.4.1.Final\hibernate-core-7.4.1.Final.jar;C:\Users\Victor\.m2\repository\jakarta\transaction\jakarta.transaction-api\2.0.1\jakarta.transaction-api-2.0.1.jar;C:\Users\Victor\.m2\repository\org\jboss\logging\jboss-logging\3.6.3.Final\jboss-logging-3.6.3.Final.jar;C:\Users\Victor\.m2\repository\org\hibernate\models\hibernate-models\1.1.1\hibernate-models-1.1.1.jar;C:\Users\Victor\.m2\repository\org\glassfish\jaxb\jaxb-runtime\4.0.9\jaxb-runtime-4.0.9.jar;C:\Users\Victor\.m2\repository\org\glassfish\jaxb\jaxb-core\4.0.9\jaxb-core-4.0.9.jar;C:\Users\Victor\.m2\repository\org\eclipse\angus\angus-activation\2.0.3\angus-activation-2.0.3.jar;C:\Users\Victor\.m2\repository\org\glassfish\jaxb\txw2\4.0.9\txw2-4.0.9.jar;C:\Users\Victor\.m2\repository\com\sun\istack\istack-commons-runtime\4.1.2\istack-commons-runtime-4.1.2.jar;C:\Users\Victor\.m2\repository\jakarta\inject\jakarta.inject-api\2.0.1\jakarta.inject-api-2.0.1.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-orm\7.0.8\spring-orm-7.0.8.jar;C:\Users\Victor\.m2\repository\org\springframework\data\spring-data-jpa\4.1.0\spring-data-jpa-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-context\7.0.8\spring-context-7.0.8.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-aop\7.0.8\spring-aop-7.0.8.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-tx\7.0.8\spring-tx-7.0.8.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-beans\7.0.8\spring-beans-7.0.8.jar;C:\Users\Victor\.m2\repository\org\antlr\antlr4-runtime\4.13.2\antlr4-runtime-4.13.2.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-aspects\7.0.8\spring-aspects-7.0.8.jar;C:\Users\Victor\.m2\repository\org\aspectj\aspectjweaver\1.9.25.1\aspectjweaver-1.9.25.1.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-jdbc\4.1.0\spring-boot-jdbc-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot\4.1.0\spring-boot-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-sql\4.1.0\spring-boot-sql-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-transaction\4.1.0\spring-boot-transaction-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-jdbc\7.0.8\spring-jdbc-7.0.8.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-webmvc\4.1.0\spring-boot-starter-webmvc-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-jackson\4.1.0\spring-boot-starter-jackson-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-jackson\4.1.0\spring-boot-jackson-4.1.0.jar;C:\Users\Victor\.m2\repository\tools\jackson\core\jackson-databind\3.1.4\jackson-databind-3.1.4.jar;C:\Users\Victor\.m2\repository\com\fasterxml\jackson\core\jackson-annotations\2.21\jackson-annotations-2.21.jar;C:\Users\Victor\.m2\repository\tools\jackson\core\jackson-core\3.1.4\jackson-core-3.1.4.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-tomcat\4.1.0\spring-boot-starter-tomcat-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-tomcat-runtime\4.1.0\spring-boot-starter-tomcat-runtime-4.1.0.jar;C:\Users\Victor\.m2\repository\org\apache\tomcat\embed\tomcat-embed-core\11.0.22\tomcat-embed-core-11.0.22.jar;C:\Users\Victor\.m2\repository\org\apache\tomcat\embed\tomcat-embed-el\11.0.22\tomcat-embed-el-11.0.22.jar;C:\Users\Victor\.m2\repository\org\apache\tomcat\embed\tomcat-embed-websocket\11.0.22\tomcat-embed-websocket-11.0.22.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-tomcat\4.1.0\spring-boot-tomcat-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-http-converter\4.1.0\spring-boot-http-converter-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-web\7.0.8\spring-web-7.0.8.jar;C:\Users\Victor\.m2\repository\io\micrometer\micrometer-observation\1.17.0\micrometer-observation-1.17.0.jar;C:\Users\Victor\.m2\repository\io\micrometer\micrometer-commons\1.17.0\micrometer-commons-1.17.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-webmvc\4.1.0\spring-boot-webmvc-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-servlet\4.1.0\spring-boot-servlet-4.1.0.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-webmvc\7.0.8\spring-webmvc-7.0.8.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-expression\7.0.8\spring-expression-7.0.8.jar;C:\Users\Victor\.m2\repository\com\mysql\mysql-connector-j\9.7.0\mysql-connector-j-9.7.0.jar;C:\Users\Victor\.m2\repository\com\h2database\h2\2.4.240\h2-2.4.240.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-starter-jdbc\4.1.0\spring-boot-starter-jdbc-4.1.0.jar;C:\Users\Victor\.m2\repository\com\zaxxer\HikariCP\7.0.2\HikariCP-7.0.2.jar;C:\Users\Victor\.m2\repository\org\slf4j\slf4j-api\2.0.18\slf4j-api-2.0.18.jar;C:\Users\Victor\.m2\repository\org\projectlombok\lombok\1.18.46\lombok-1.18.46.jar;C:\Users\Victor\.m2\repository\jakarta\xml\bind\jakarta.xml.bind-api\4.0.5\jakarta.xml.bind-api-4.0.5.jar;C:\Users\Victor\.m2\repository\jakarta\activation\jakarta.activation-api\2.1.4\jakarta.activation-api-2.1.4.jar;C:\Users\Victor\.m2\repository\net\bytebuddy\byte-buddy\1.18.10\byte-buddy-1.18.10.jar;C:\Users\Victor\.m2\repository\org\springframework\spring-core\7.0.8\spring-core-7.0.8.jar;C:\Users\Victor\.m2\repository\commons-logging\commons-logging\1.3.6\commons-logging-1.3.6.jar;C:\Users\Victor\.m2\repository\org\jspecify\jspecify\1.0.0\jspecify-1.0.0.jar;C:\Users\Victor\.m2\repository\org\springframework\boot\spring-boot-web-server\4.1.0\spring-boot-web-server-4.1.0.jar" com.example.demo.DemoApplication



  .   ____          _            __ _ _

 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \

( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \

 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )

  '  |____| .__|_| |_|_| |_\__, | / / / /

 =========|_|==============|___/=/_/_/_/



 :: Spring Boot ::                (v4.1.0)



2026-06-21T17:08:02.155-03:00  INFO 6396 --- [           main] com.example.demo.DemoApplication         : Starting DemoApplication using Java 17.0.12 with PID 6396 (C:\Users\Victor\Documents\STGT backend\target\classes started by Victor in C:\Users\Victor\Documents\STGT backend)

2026-06-21T17:08:02.160-03:00  INFO 6396 --- [           main] com.example.demo.DemoApplication         : No active profile set, falling back to 1 default profile: "default"

2026-06-21T17:08:02.772-03:00  INFO 6396 --- [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data JPA repositories in DEFAULT mode.

2026-06-21T17:08:02.823-03:00  INFO 6396 --- [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 41 ms. Found 1 JPA repository interface.

2026-06-21T17:08:03.315-03:00  INFO 6396 --- [           main] o.s.boot.tomcat.TomcatWebServer          : Tomcat initialized with port 8080 (http)

2026-06-21T17:08:03.331-03:00  INFO 6396 --- [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]

2026-06-21T17:08:03.331-03:00  INFO 6396 --- [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/11.0.22]

2026-06-21T17:08:03.387-03:00  INFO 6396 --- [           main] b.w.c.s.WebApplicationContextInitializer : Root WebApplicationContext: initialization completed in 1168 ms

2026-06-21T17:08:03.576-03:00  INFO 6396 --- [           main] org.hibernate.orm.jpa                    : HHH008540: Processing PersistenceUnitInfo [name: default]

2026-06-21T17:08:03.654-03:00  INFO 6396 --- [           main] org.hibernate.orm.core                   : HHH000001: Hibernate ORM core version 7.4.1.Final

2026-06-21T17:08:04.094-03:00  INFO 6396 --- [           main] o.s.o.j.p.SpringPersistenceUnitInfo      : No LoadTimeWeaver setup: ignoring JPA class transformer

2026-06-21T17:08:04.130-03:00  INFO 6396 --- [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Starting...

2026-06-21T17:08:04.268-03:00  INFO 6396 --- [           main] com.zaxxer.hikari.pool.HikariPool        : HikariPool-1 - Added connection conn0: url=jdbc:h2:mem:stgtdb user=SA

2026-06-21T17:08:04.270-03:00  INFO 6396 --- [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Start completed.

2026-06-21T17:08:04.289-03:00  WARN 6396 --- [           main] org.hibernate.orm.deprecation            : HHH90000025: H2Dialect does not need to be specified explicitly using 'hibernate.dialect' (remove the property setting and it will be selected by default)

2026-06-21T17:08:04.312-03:00  INFO 6396 --- [           main] org.hibernate.orm.connections.pooling    : HHH10001005: Database info:

Database JDBC URL [jdbc:h2:mem:stgtdb]

Database driver: H2 JDBC Driver

Database dialect: H2Dialect

Database version: 2.4.240

Default catalog/schema: STGTDB/PUBLIC

Autocommit mode: undefined/unknown

Isolation level: READ_COMMITTED [default READ_COMMITTED]

JDBC fetch size: 100

Pool: DataSourceConnectionProvider

Minimum pool size: undefined/unknown

Maximum pool size: undefined/unknown

2026-06-21T17:08:04.902-03:00  INFO 6396 --- [           main] org.hibernate.orm.core                   : HHH000489: No JTA platform available (set 'hibernate.transaction.jta.platform' to enable JTA platform integration)

2026-06-21T17:08:04.964-03:00  INFO 6396 --- [           main] j.LocalContainerEntityManagerFactoryBean : Initialized JPA EntityManagerFactory for persistence unit 'default'

2026-06-21T17:08:05.033-03:00  INFO 6396 --- [           main] o.s.d.j.r.query.QueryEnhancerFactories   : Hibernate is in classpath; If applicable, HQL parser will be used.

2026-06-21T17:08:05.181-03:00  WARN 6396 --- [           main] JpaBaseConfiguration$JpaWebConfiguration : spring.jpa.open-in-view is enabled by default. Therefore, database queries may be performed during view rendering. Explicitly configure spring.jpa.open-in-view to disable this warning

2026-06-21T17:08:05.512-03:00  INFO 6396 --- [           main] o.s.boot.tomcat.TomcatWebServer          : Tomcat started on port 8080 (http) with context path '/'

2026-06-21T17:08:05.519-03:00  INFO 6396 --- [           main] com.example.demo.DemoApplication         : Started DemoApplication in 3.811 seconds (process running for 4.209)

atualize o pom e rodei, este eh o log

O log mostra que o servidor subiu perfeitamente em 3.811 segundos, a conexão com o banco de dados H2 foi estabelecida com sucesso ("HikariPool-1 - Start completed"), mas a linha do console ainda não apareceu.

Isso significa que a propriedade de ativação do console que colocamos no arquivo não está sendo lida pelo Spring Boot, muito provavelmente por causa do cache do IntelliJ.

Como queremos testar se os dados do Postman estão realmente sendo salvos, existe uma forma muito mais rápida e direta de visualizar o banco de dados sem depender do navegador web: usando o próprio painel de banco de dados integrado do IntelliJ IDEA.

Como ver o banco H2 direto no IntelliJ:
Deixe o servidor rodando (não aperte stop).

Olhe para o lado direito da tela do seu IntelliJ. Você verá uma aba vertical escrita Database. Clique nela.
(Se não estiver lá, vá no menu superior View -> Tool Windows -> Database).

No painel Database que abrir, clique no ícone de + (New) -> Data Source -> H2.

Uma janela de configuração vai abrir. Preencha assim:

Connection Type: In-memory

URL: Apague o que estiver lá e cole exatamente a sua: jdbc:h2:mem:stgtdb

User: sa

Password: password

Na parte inferior da janela, pode aparecer um aviso amarelo pedindo para baixar os drivers ("Download missing driver files"). Se aparecer, clique em "Download".

Clique no botão Test Connection (na parte inferior da tela). Deve aparecer uma mensagem verde de sucesso.

Clique em OK.

Vendo os dados do seu Foguete:
Agora, no painel lateral Database, você verá sua conexão stgtdb (ou default). Expanda as pastas (clique nas setinhas) seguindo este caminho: stgtdb -> STGTDB -> PUBLIC -> tables.

Você verá a tabela telemetry_logs ali!

Dê um duplo clique nela.

O IntelliJ vai abrir uma aba no centro da tela (onde você edita código) parecida com uma planilha do Excel.

Agora, vá no seu Postman, aperte Send algumas vezes na rota POST para mandar dados fictícios e, na aba da planilha do IntelliJ, clique no ícone de recarregar (duas setas em círculo). Você verá os dados aparecendo na tabela em tempo real.

Conseguiu ver os dados populando a tabela por aí?

nao vi essa opcao database

Pela sua imagem, acabei de reparar num detalhe importante: no canto superior direito do seu ecrã diz "Unlock Ultimate".

Isso explica tudo! A aba "Database" integrada é uma funcionalidade exclusiva da versão Ultimate (paga) do IntelliJ. Como está a usar a versão Community (gratuita), essa ferramenta nativa não está disponível.

Mas não há qualquer problema! Na verdade, como você construiu uma API REST completa, a forma mais correta e profissional de verificar se os dados foram guardados na base de dados é usar a sua própria rota GET no Postman.

Vamos fazer o ciclo completo de voo pelo Postman. Siga estes passos exatamente:

Passo 1: Enviar os dados do "Foguete" (POST)
Certifique-se de que o servidor está a correr no IntelliJ (botão Play verde).

Abra o Postman e abra um novo separador (botão +).

Mude o método para POST.

Insira o URL: http://localhost:8080/api/telemetry

Vá à aba Body, selecione raw e mude o tipo de texto para JSON.

Cole o seguinte pacote:

JSON
{
  "temperaturaAmbiente": 25.5,
  "temperaturaSuperficial": 82.1,
  "timestampVoo": 1500
}
Clique em Send.
(Deve receber um Status 200 OK e ver o pacote devolvido na resposta).

Faça isto 3 vezes, alterando ligeiramente os números das temperaturas e do timestamp, para simular que o foguete está a enviar vários pacotes durante a subida.

Passo 2: Visualizar a Base de Dados (GET)
Agora vamos pedir à base de dados que nos mostre tudo o que guardou. É isto que o seu futuro frontend vai fazer para desenhar os gráficos!

No Postman, abra um novo separador (botão +).

Deixe o método como GET.

Insira o mesmo URL: http://localhost:8080/api/telemetry

Clique em Send (não precisa de colocar nada no Body).

O Resultado:
Na área de resposta em baixo, verá um array JSON (uma lista [ ]) com todos os pacotes que enviou no Passo 1. Cada um terá um "id" único gerado sequencialmente pela base de dados H2 e a "dataRecebimento" exata em que o servidor guardou o dado.

Se esta lista aparecer com os seus dados, parabéns! O seu sistema de backend está a funcionar a 100%, a receber os dados do simulador, a processá-los no Service e a guardá-los com sucesso na base de dados H2 através do Repository.

Conseguiu ver a lista completa a aparecer no GET do Postman?

#include <WiFi.h>

#include <esp_now.h>

#include <esp_wifi.h>

#include <Wire.h>

#include <Adafruit_GFX.h>

#include <Adafruit_SSD1306.h>



// ==========================================

// CONFIGURAÇÕES DA TELA OLED

// ==========================================

#define SCREEN_WIDTH 128

#define SCREEN_HEIGHT 64

#define OLED_RESET    -1

#define SCREEN_ADDRESS 0x3C // Endereço padrão I2C



Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);



// ==========================================

// 1. ESTRUTURA DE DADOS (Idêntica ao Emissor)

// ==========================================

typedef struct data_log {

  double temperaturaAmbiente;

  double temperaturaSuperficial;

  unsigned long timestamp;

} data_log;



data_log payloadRecebido;



// Flag (sinalizador) para avisar o loop que chegou dado novo

volatile bool novaLeitura = false; 



// ==========================================

// 2. CALLBACK DE RECEPÇÃO ESP-NOW

// ==========================================

void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {

  // Copia os dados

  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));

  

  Serial.println("=== Novo Pacote Recebido ===");

  Serial.print("Temp. Superficial: "); Serial.print(payloadRecebido.temperaturaSuperficial); Serial.println(" °C");

  Serial.print("Temp. Ambiente:    "); Serial.print(payloadRecebido.temperaturaAmbiente); Serial.println(" °C");

  Serial.print("Tempo de Voo:      "); Serial.print(payloadRecebido.timestamp); Serial.println(" ms");

  Serial.println("============================\n");



  novaLeitura = true; 

}



// ==========================================

// 3. FUNÇÃO PARA DESENHAR NO OLED

// ==========================================

void atualizarDisplayOLED() {

  display.clearDisplay(); // Limpa o quadro antigo



  display.setTextSize(1);

  display.setTextColor(SSD1306_WHITE);

  display.setCursor(0, 0);

  display.print("EQUIPE ROCKET - STGT");

  

  // Linha separadora horizontal

  display.drawLine(0, 10, 128, 10, SSD1306_WHITE); 



  display.setTextSize(1);

  display.setCursor(0, 15);

  display.print("T. Superficial:");

  

  display.setTextSize(2); // Fonte grande!

  display.setCursor(0, 25);

  display.print(payloadRecebido.temperaturaSuperficial, 1); // 1 casa decimal

  display.print(" C");



  display.setTextSize(1); // Volta para a fonte menor

  display.setCursor(0, 45);

  display.print("T. Amb: ");

  display.print(payloadRecebido.temperaturaAmbiente, 1);

  display.print(" C");



  display.setCursor(0, 55);

  display.print("Status: ");

  

  bool alertaAtivo = payloadRecebido.temperaturaSuperficial >= 80.0;



  if (alertaAtivo) {

    display.setTextColor(SSD1306_BLACK, SSD1306_WHITE); 

    display.print(" CRITICO ");

    display.setTextColor(SSD1306_WHITE); // Volta a cor ao normal

  } else {

    display.print("NOMINAL");

  }



  display.display(); 

}



// ==========================================

// SETUP E LOOP PRINCIPAL

// ==========================================

void setup() {

  Serial.begin(115200);





  if(!display.begin(SSD1306_SWITCHCAPVCC, SCREEN_ADDRESS)) {

    Serial.println(F("Falha ao inicializar o OLED SSD1306"));

    for(;;); // Trava o código aqui se o display estiver desconectado

  }

  

  // Animação de Boot da Equipe

  display.clearDisplay();

  display.setTextSize(1);

  display.setTextColor(SSD1306_WHITE);

  display.setCursor(15, 20);

  display.println("STGT - Base Solo");

  display.setCursor(25, 35);

  display.println("Iniciando...");

  display.display();

  delay(2000); 



  WiFi.mode(WIFI_STA);

  esp_wifi_set_channel(1, WIFI_SECOND_CHAN_NONE);



  Serial.println("\n--- Iniciando Receptor de Telemetria ---");

  Serial.print("Endereço MAC da Base: ");

  Serial.println(WiFi.macAddress()); 



  if (esp_now_init() != ESP_OK) {

    Serial.println("Erro Crítico: Falha ao inicializar o ESP-NOW");

    display.clearDisplay();

    display.setCursor(0, 20);

    display.print("ERRO DE RADIO!");

    display.display();

    return;

  }

  

  esp_now_register_recv_cb((esp_now_recv_cb_t)OnDataRecv);

  

  Serial.println("Receptor pronto e escutando a frequência...\n");



  // Tela de espera

  display.clearDisplay();

  display.setCursor(0, 25);

  display.print("Aguardando Foguete...");

  display.display();

}



void loop() {

  if (novaLeitura == true) {

    atualizarDisplayOLED();

    novaLeitura = false;    // Abaixa a flag para esperar o próximo pacote

  }

  

  delay(10); 

} otimo agora quero ajustar o codigo do receptor para conseguer emitir o request http, eu vou atualizar ele direto via github, entao nao eh pra dar erros

Aqui está o código atualizado do Receptor (Base de Solo), estruturado para ser robusto e livre de erros para a sua atualização direta no GitHub.

Adicionei a biblioteca nativa HTTPClient.h e criei uma função dedicada para montar o JSON e disparar o POST sem travar o loop principal.

Código Completo (Receptor com HTTP POST)
C++
#include <WiFi.h>
#include <esp_now.h>
#include <esp_wifi.h>
#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>
#include <HTTPClient.h> // Biblioteca necessária para requisições HTTP

// ==========================================
// CONFIGURAÇÕES DA TELA OLED
// ==========================================
#define SCREEN_WIDTH 128
#define SCREEN_HEIGHT 64
#define OLED_RESET    -1
#define SCREEN_ADDRESS 0x3C

Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

// ==========================================
// CONFIGURAÇÕES DE REDE E SERVIDOR
// ==========================================
// INSIRA OS DADOS DO SEU WI-FI (Pode ser o roteador do celular)
const char* ssid = "NOME_DO_SEU_WIFI";
const char* password = "SENHA_DO_SEU_WIFI";

// IMPORTANTE: Troque "192.168.X.X" pelo IP local da sua máquina (IPv4)
const String serverName = "http://192.168.X.X:8080/api/telemetry";

// ==========================================
// 1. ESTRUTURA DE DADOS
// ==========================================
typedef struct data_log {
  double temperaturaAmbiente;
  double temperaturaSuperficial;
  unsigned long timestamp;
} data_log;

data_log payloadRecebido;

// Flag de segurança para o loop
volatile bool novaLeitura = false; 

// ==========================================
// 2. CALLBACK DE RECEPÇÃO ESP-NOW
// ==========================================
void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {
  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));
  
  Serial.println("=== Novo Pacote Recebido ===");
  Serial.print("Temp. Superficial: "); Serial.print(payloadRecebido.temperaturaSuperficial); Serial.println(" °C");
  Serial.print("Tempo de Voo:      "); Serial.print(payloadRecebido.timestamp); Serial.println(" ms");
  Serial.println("============================\n");

  novaLeitura = true; 
}

// ==========================================
// 3. FUNÇÃO PARA DISPARAR O HTTP POST
// ==========================================
void enviarParaNuvem() {
  if (WiFi.status() == WL_CONNECTED) {
    HTTPClient http;
    http.begin(serverName);
    http.addHeader("Content-Type", "application/json");

    // Monta a string JSON manualmente (mais rápido e não exige a biblioteca ArduinoJson)
    char jsonBuffer[128];
    snprintf(jsonBuffer, sizeof(jsonBuffer), 
             "{\"temperaturaAmbiente\":%.2f,\"temperaturaSuperficial\":%.2f,\"timestampVoo\":%lu}", 
             payloadRecebido.temperaturaAmbiente, 
             payloadRecebido.temperaturaSuperficial, 
             payloadRecebido.timestamp);

    // Dispara a requisição
    int httpResponseCode = http.POST(jsonBuffer);

    if (httpResponseCode > 0) {
      Serial.print("HTTP POST enviado! Código de Resposta: ");
      Serial.println(httpResponseCode);
    } else {
      Serial.print("Erro Crítico no HTTP POST. Código: ");
      Serial.println(httpResponseCode);
    }
    
    http.end(); // Libera os recursos
  } else {
    Serial.println("Aviso: Wi-Fi desconectado. Pacote salvo apenas localmente.");
  }
}

// ==========================================
// 4. FUNÇÃO PARA DESENHAR NO OLED
// ==========================================
void atualizarDisplayOLED() {
  display.clearDisplay(); 
  display.setTextSize(1);
  display.setTextColor(SSD1306_WHITE);
  display.setCursor(0, 0);
  display.print("EQUIPE ROCKET - STGT");
  
  display.drawLine(0, 10, 128, 10, SSD1306_WHITE); 

  display.setTextSize(1);
  display.setCursor(0, 15);
  display.print("T. Superficial:");
  
  display.setTextSize(2); 
  display.setCursor(0, 25);
  display.print(payloadRecebido.temperaturaSuperficial, 1); 
  display.print(" C");

  display.setTextSize(1); 
  display.setCursor(0, 45);
  display.print("T. Amb: ");
  display.print(payloadRecebido.temperaturaAmbiente, 1);
  display.print(" C");

  display.setCursor(0, 55);
  display.print("Status: ");
  
  bool alertaAtivo = payloadRecebido.temperaturaSuperficial >= 80.0;

  if (alertaAtivo) {
    display.setTextColor(SSD1306_BLACK, SSD1306_WHITE); 
    display.print(" CRITICO ");
    display.setTextColor(SSD1306_WHITE); 
  } else {
    display.print("NOMINAL");
  }
  display.display(); 
}

// ==========================================
// SETUP E LOOP PRINCIPAL
// ==========================================
void setup() {
  Serial.begin(115200);

  if(!display.begin(SSD1306_SWITCHCAPVCC, SCREEN_ADDRESS)) {
    Serial.println(F("Falha ao inicializar o OLED SSD1306"));
    for(;;); 
  }
  
  // Animação de Boot
  display.clearDisplay();
  display.setTextSize(1);
  display.setTextColor(SSD1306_WHITE);
  display.setCursor(15, 20);
  display.println("STGT - Base Solo");
  display.setCursor(25, 35);
  display.println("Conectando Rede...");
  display.display();

  // 1. Conexão Wi-Fi (Necessária para o HTTP)
  WiFi.mode(WIFI_STA);
  WiFi.begin(ssid, password);
  
  Serial.print("Conectando ao Wi-Fi");
  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }
  Serial.println("\nWi-Fi Conectado!");
  Serial.print("IP da Base: ");
  Serial.println(WiFi.localIP());

  // 2. Configuração do Rádio ESP-NOW
  // O comando esp_wifi_set_channel foi removido aqui. O ESP32 assumirá 
  // automaticamente o canal em que o roteador Wi-Fi estiver operando.
  
  Serial.println("\n--- Iniciando Receptor de Telemetria ---");
  Serial.print("Endereço MAC da Base: ");
  Serial.println(WiFi.macAddress()); 

  if (esp_now_init() != ESP_OK) {
    Serial.println("Erro Crítico: Falha ao inicializar o ESP-NOW");
    return;
  }
  
  esp_now_register_recv_cb((esp_now_recv_cb_t)OnDataRecv);
  
  display.clearDisplay();
  display.setCursor(0, 25);
  display.print("Aguardando Voo...");
  display.display();
}

void loop() {
  if (novaLeitura == true) {
    atualizarDisplayOLED();
    enviarParaNuvem();      // Dispara o JSON para o Spring Boot
    novaLeitura = false;    
  }
  delay(10); 
}
⚠️ Alertas Críticos Antes de Rodar
O Problema do localhost: O seu ESP32 é uma máquina independente na rede; ele não sabe o que é localhost (para ele, localhost é ele mesmo). Você precisa abrir o terminal do seu computador (CMD no Windows), digitar ipconfig e copiar o Endereço IPv4 (ex: 192.168.1.15). Cole esse IP na variável serverName do código.

A Armadilha do Canal de Rádio: Repare que retirei a linha esp_wifi_set_channel(1, WIFI_SECOND_CHAN_NONE); do setup. Como agora o ESP32 Base está conectado a um roteador de internet, ele será forçado a operar no canal que o roteador mandar (pode ser o 6, o 11, etc.). Se o código do seu Foguete ainda estiver "travado" no Canal 1, a base ficará surda para ele.

A Solução de Bancada: Roteie o Wi-Fi do seu próprio celular em 2.4GHz e conecte tanto a Base de Solo quanto o seu computador (rodando o Java) nele. Certifique-se de alterar o código do Foguete para transmitir no mesmo canal que o celular escolher, ou configure o roteador para operar em um canal fixo.

Ótimo, agora quero colocar tudono mesmo repo do github : o que devo deletar do meu stgt backend para copiar e colar a pasta dele e transformar em um monorpeo funcional?

Transformar o projeto num monorepo (um único repositório no GitHub contendo o código do hardware e do software) é uma excelente decisão de engenharia. Isso garante que a versão da sua API (Java) esteja sempre sincronizada com a versão do pacote de dados (C++) que o ESP32 está enviando.

Olhando para a sua pasta STGT backend, você precisa fazer uma "limpeza" antes de arrastar esses arquivos para o repositório oficial.

1. O que você DEVE deletar:
Pasta target: (Obrigatório) Esta pasta contém os arquivos compilados (.class e .jar). Ela é gerada automaticamente pelo Maven toda vez que você roda o projeto. Ela é pesada e nunca deve ir para o GitHub.

Pasta .idea: (Altamente Recomendado) Contém as configurações locais do seu IntelliJ (onde as janelas estão abertas, caminhos específicos do seu PC). Se você enviar isso para o GitHub e outra pessoa (ou você em outro PC) baixar, pode dar conflito.

Pasta oculta .git: (Crítico) Se você iniciou um repositório git dentro dessa pasta STGT backend isoladamente, você precisa apagar a pasta oculta .git que está aí dentro. Se você não fizer isso, o GitHub vai tratar o seu backend como um Submódulo e você não conseguirá ver os arquivos pela interface web. (Dica: no Windows, clique em "Visualizar" -> "Mostrar" -> "Itens ocultos" para garantir que ela não está lá escondida).

2. O que você DEVE manter:
src (Onde está o seu código Java de fato).

pom.xml (A receita de dependências do Maven).

.mvn, mvnw, mvnw.cmd (O "motor" do Maven embutido, garante que o código rode em qualquer PC mesmo sem o Maven instalado).

.gitignore (O arquivo que vai impedir que as pastas target e .idea voltem a ser "vistas" pelo Git no futuro).

3. Como deve ficar a Estrutura do seu Monorepo:
No seu repositório oficial do GitHub (aquele que você já usa para o ESP32), crie duas pastas raízes claras para separar as responsabilidades. Arraste os arquivos limpos do backend para dentro da pasta backend.

A estrutura ideal ficará assim:

Plaintext
Sistema-de-Telemetria-de-Baixo-Custo/ (Raiz do repositório)
│
├── esp32/                        # Seu código C++ atual
│   ├── foguete/
│   │   └── emissor.ino
│   └── base_solo/
│       └── receptor.ino
│
├── backend/                      # Arraste o conteúdo do Spring Boot pra cá
│   ├── src/
│   ├── .mvn/
│   ├── pom.xml
│   ├── mvnw
│   └── .gitignore                # O gitignore específico do Java
│
└── README.md                     # Documentação geral do STGT
Após fazer essa organização no seu computador, basta abrir o terminal na raiz do repositório (Sistema-de-Telemetria...), dar um git add ., git commit -m "feat: adicionado backend spring boot ao monorepo" e git push.

ele esta assim atualmente

Perfeito! O seu repositório já está bem encaminhado com as pastas do hardware (codigo_emissor e codigo_receptor). Para transformá-lo num monorepo impecável e subir o seu backend em Java, vamos fazer isso de forma limpa e organizada.

Aproveitando, notei na sua imagem que a pasta do receptor tem um subdiretório com um nome bem longo (codigo_receptor_copy_20260...). Recomendo renomear apenas para codigo_receptor na sua máquina local antes de subir, para manter o padrão profissional.

Aqui está o passo a passo exato do que você deve fazer na sua máquina local para juntar tudo e enviar para esse repositório:

Passo 1: Limpar a pasta do Backend (Crucial)
Vá na sua pasta original do STGT backend (aquela da sua imagem anterior) e delete:

A pasta target (pesada e desnecessária no GitHub).

A pasta .idea (configurações locais do seu IntelliJ).

A pasta oculta .git (Se ela existir aí dentro, tem que ser deletada. Caso contrário, o GitHub não vai ler os seus arquivos Java e vai transformar a pasta num "submódulo" quebrado).

Passo 2: Juntar tudo na mesma casa
Abra a pasta local do seu repositório Sistema-de-Telemetria-de-Baixo-Custo (a pasta que está sincronizada com esse GitHub da imagem).

Crie uma nova pasta vazia chamada backend (ou api_spring_boot, como preferir) ao lado das pastas codigo_emissor e codigo_receptor.

Pegue todos os arquivos limpos do seu STGT backend (src, pom.xml, .mvn, etc.) e mova/cole para dentro dessa nova pasta backend.

Sua pasta local do projeto ficará exatamente assim:

Plaintext
Sistema-de-Telemetria-de-Baixo-Custo/
├── codigo_emissor/
├── codigo_receptor/
└── backend/
    ├── src/
    ├── .mvn/
    ├── pom.xml
    └── ...
Passo 3: Enviar para o GitHub
Abra o terminal (ou Prompt de Comando/Git Bash) dentro da pasta raiz Sistema-de-Telemetria-de-Baixo-Custo e digite os seguintes comandos, um por um:

git add . (isso vai adicionar a pasta do backend nova e a renomeação do receptor)

git commit -m "feat: adiciona backend em spring boot e organiza monorepo"

git push

Ao atualizar a página do GitHub da sua imagem, a nova pasta backend estará lá, lindamente sincronizada com o seu código C++.

Quer que eu já monte um template de README.md bem estruturado sobre o STGT para você colocar na raiz do projeto e deixar o repositório com "cara de portfólio"?

antes disso, preciso clonar este repo para meu notebook, ele esta salvo em outro atualemnte, como posso fazer?

Clonar o repositório para o seu notebook novo é um processo rápido e direto. Como o seu projeto já está na nuvem (GitHub), você só precisa "puxar" essa pasta para a máquina nova.

Aqui está o passo a passo exato do que você deve fazer no seu notebook:

Passo 1: Verificar se o Git está instalado
Antes de clonar, o seu notebook precisa ter o Git instalado.

Abra o Terminal (Prompt de Comando ou PowerShell no Windows).

Digite: git --version e aperte Enter.

Se aparecer a versão do Git (ex: git version 2.41.0), pode seguir para o Passo 2. Se der erro de "comando não reconhecido", baixe e instale o Git rapidamente pelo site oficial (git-scm.com).

Passo 2: Copiar o link do repositório
Abra o navegador no seu notebook e acesse a página do seu repositório no GitHub.

Clique no botão verde "<> Code".

Na aba Local > HTTPS, clique no ícone de copiar ao lado do link. (Será algo como: https://github.com/victorfares/Sistema-de-Telemetria-de-Baixo-Custo.git).

Passo 3: Clonar para a máquina nova
No seu notebook, abra o Terminal (ou Git Bash).

Navegue até a pasta onde você quer salvar o projeto. Por exemplo, para salvar na pasta Documentos, digite:
cd Documents

Agora, digite o comando git clone seguido de um espaço e cole o link que você copiou:
git clone https://github.com/victorfares/Sistema-de-Telemetria-de-Baixo-Custo.git

Aperte Enter.

O Git vai baixar toda a estrutura de pastas, o código do emissor, do receptor e os históricos de commits perfeitamente.

Depois disso, basta abrir a pasta backend pelo IntelliJ IDEA e os arquivos .ino pela Arduino IDE ou VS Code no seu notebook.

perfeito, ele esta aqui agora, eu so preciso deletar as pastas desncessarias do backend, ecriar a pasta back end no repo, e dar push?

Exatamente! Você pegou a ideia perfeitamente. É apenas um trabalho de "copiar e colar" com organização.

Para não ter erro, aqui está o checklist final de 3 passos que você vai executar agora no seu notebook:

1. A Limpeza (na pasta STGT backend antiga)
Vá na pasta original do seu backend e delete:

[ ] A pasta target

[ ] A pasta .idea

[ ] A pasta oculta .git (Extremamente importante: se não deletar essa, o GitHub vai bugar e não vai mostrar seus arquivos de código).

2. A Movimentação (na pasta do Repositório Clonado)
[ ] Abra a pasta do repositório que você acabou de clonar (Sistema-de-Telemetria-de-Baixo-Custo).

[ ] Crie uma pasta vazia chamada backend lá dentro.

[ ] Copie tudo o que sobrou da sua pasta STGT backend (a pasta src, o pom.xml, etc.) e cole dentro dessa nova pasta backend.

3. O Push para a Nuvem
Abra o terminal dentro da pasta raiz do seu repositório clonado e digite:

git add .

git commit -m "feat: adiciona backend em spring boot e organiza monorepo"

git push

Assim que o push terminar, se você atualizar a página do GitHub, verá o seu monorepo profissional pronto!

Me avise quando terminar esse processo. Se der tudo certo, já te mando o template matador do README.md para coroar o seu projeto!

otimo, esta feito, agora quero que vc gere uma documentacao COMPLETA DO PROJETO, DIZENDO O QUE ELE FAZ COMO FUNCIONA (COM SNIPPETS DE CODIGOS MAIS FUDNMENTAIS) E EXEMPLO DE COMO TESTAR A APLICACAO (POSTS COM O JSON PREPARADO PARA AA PI SPRING BOOT)

Aqui está o template completo e profissional para o seu README.md. Ele foi desenhado para ser lido tanto por avaliadores acadêmicos quanto por recrutadores técnicos, detalhando a arquitetura, o fluxo de dados e como rodar o sistema.

Basta copiar todo o texto abaixo e colar no arquivo README.md na raiz do seu repositório.

🚀 STGT - Sistema de Telemetria e Gerenciamento Térmico de Baixo Custo
📌 Sobre o Projeto
O STGT (Sistema de Telemetria e Gerenciamento Térmico) é uma solução de engenharia desenvolvida pela Equipe Rocket para resolver o problema do "Voo Cego" em foguetemodelismo. Durante o voo, o atrito aerodinâmico e o calor da propulsão (motor KNSB) podem causar falhas catastróficas na eletrônica embarcada (Thermal Throttling e derretimento de trilhas).

Utilizando a filosofia COTS (Commercial Off-The-Shelf), este projeto substitui altímetros e sistemas de telemetria comerciais de alto custo (que ultrapassam R$ 4.500) por uma arquitetura acessível e de alta performance baseada no microcontrolador ESP32.

🏗️ Arquitetura do Sistema
O projeto é dividido em três camadas principais:

Camada Embarcada (Foguete): Lê a temperatura ambiente e superficial via I2C (Sensor GY-906) e transmite os dados estruturados via rádio frequência (2.4 GHz) em tempo real.

Camada de Solo (Receptor): Capta os pacotes via rádio, exibe alertas visuais críticos numa tela OLED e atua como um Gateway, roteando os dados via Wi-Fi para a nuvem.

Camada de Nuvem (API Backend): Um servidor RESTful em Java (Spring Boot) que recebe, valida e persiste os logs de voo em um banco de dados relacional para futura plotagem gráfica.

⚙️ Como Funciona (Snippets de Código)
1. Comunicação Ponto a Ponto (ESP-NOW)
Para evitar a latência de redes Wi-Fi tradicionais, utilizamos o protocolo ESP-NOW. Os dados trafegam em pacotes binários crus através de uma struct C++, garantindo máxima velocidade de amostragem.

C++
// Molde do pacote que trafega pelo ar (C++)
typedef struct data_log {
  double temperaturaAmbiente;
  double temperaturaSuperficial;
  unsigned long timestamp; // Tempo de voo em milissegundos
} data_log;
2. Tratamento Assíncrono na Base de Solo
A base recebe o sinal via interrupção de hardware e delega o desenho da tela OLED de forma assíncrona, evitando que o Gateway trave (Kernel Panic) ao receber muitos dados.

C++
// Interrupção ativada instantaneamente quando o pacote de rádio chega
void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {
  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));
  novaLeitura = true; // Levanta a flag segura de forma não-bloqueante
}
3. Persistência na API Spring Boot
Os dados do Gateway são recebidos no Backend através de um Controlador REST, que converte o JSON recebido num Data Transfer Object (DTO) e o envia para o banco de dados via Spring Data JPA.

Java
// Porta de entrada do Servidor (Java)
@PostMapping
public ResponseEntity<TelemetryLog> receberDados(@RequestBody TelemetryRequestDTO dto) {
    TelemetryLog logSalvo = service.salvarTelemetria(dto);
    System.out.println("Pacote salvo: Temp Superficial = " + logSalvo.getTemperaturaSuperficial() + "°C");
    return ResponseEntity.ok(logSalvo);
}
🛠️ Como Testar e Rodar a Aplicação
Requisitos
Java 17+ instalado.

Arduino IDE (com pacotes do ESP32 e bibliotecas Adafruit instaladas).

Postman ou Insomnia (para testar a API sem o hardware).

Passo 1: Subindo a API (Backend)
Navegue até a pasta backend/.

Abra o terminal e rode o comando do Maven para iniciar o servidor:

Bash
./mvnw spring-boot:run
O servidor iniciará na porta 8080 com o banco de dados H2 rodando em memória.

Passo 2: Simulando o Foguete no Postman
Se você não estiver com as placas físicas, pode simular o envio dos dados de telemetria enviando um HTTP POST.

URL: http://localhost:8080/api/telemetry

Método: POST

Headers: Content-Type: application/json

Body (JSON):

JSON
{
  "temperaturaAmbiente": 28.5,
  "temperaturaSuperficial": 85.2,
  "timestampVoo": 12500
}
Nota: Ao enviar este POST, o servidor responderá com o Status 200 OK e o registro será salvo automaticamente com a data e hora do recebimento.

Passo 3: Consultando o Histórico de Voo
Para ver o histórico completo de pacotes (ideal para o frontend desenhar gráficos):

URL: http://localhost:8080/api/telemetry

Método: GET

Passo 4: Rodando o Hardware (ESP32)
Substitua o Endereço MAC (no código do Emissor) pelo MAC da sua placa Receptora.

No código do Receptor, atualize as credenciais do Wi-Fi (ssid e password) e substitua 192.168.X.X pelo IP local da máquina que está rodando o Spring Boot.

Conecte os sensores (Pinos I2C) e faça o upload.

Assim que o foguete for energizado, os dados aparecerão na tela OLED e no console do backend.

💻 Próximos Passos (Roadmap)
[x] Leitura de sensores infravermelhos de alta precisão (GY-906).

[x] Comunicação rádio-frequência ponto a ponto (ESP-NOW).

[x] Alertas visuais operacionais (Display OLED de Solo).

[x] API RESTful Java conectada a banco de dados relacional.

[ ] Desenvolvimento de uma Dashboard Frontend reativa (Vue.js / Vuetify).

VC APENAS DIGITOU AS PALAVRAS, MANDE E LE FORMATADA PARA MD

Peço desculpas pela confusão! Aqui está o código exatamente formatado em Markdown cru.

Basta você clicar no botão de "Copiar código" (no canto superior direito do bloco abaixo) e colar diretamente no arquivo README.md do seu repositório:

Markdown
# 🚀 STGT - Sistema de Telemetria e Gerenciamento Térmico de Baixo Custo

![Badge Status](https://img.shields.io/badge/Status-Em%20Desenvolvimento-green)
![Badge Hardware](https://img.shields.io/badge/Hardware-ESP32%20%7C%20OLED%20%7C%20GY--906-blue)
![Badge Backend](https://img.shields.io/badge/Backend-Java%2017%20%7C%20Spring%20Boot-brightgreen)

## 📌 Sobre o Projeto
O **STGT (Sistema de Telemetria e Gerenciamento Térmico)** é uma solução de engenharia desenvolvida pela Equipe Rocket para resolver o problema do "Voo Cego" em foguetemodelismo. Durante o voo, o atrito aerodinâmico e o calor da propulsão (motor KNSB) podem causar falhas catastróficas na eletrônica embarcada (*Thermal Throttling* e derretimento de trilhas).

Utilizando a filosofia **COTS (Commercial Off-The-Shelf)**, este projeto substitui altímetros e sistemas de telemetria comerciais de alto custo (que ultrapassam R$ 4.500) por uma arquitetura acessível e de alta performance baseada no microcontrolador ESP32.

## 🏗️ Arquitetura do Sistema
O projeto é dividido em três camadas principais:

1. **Camada Embarcada (Foguete):** Lê a temperatura ambiente e superficial via I2C (Sensor GY-906) e transmite os dados estruturados via rádio frequência (2.4 GHz) em tempo real.
2. **Camada de Solo (Receptor):** Capta os pacotes via rádio, exibe alertas visuais críticos numa tela OLED e atua como um *Gateway*, roteando os dados via Wi-Fi para a nuvem.
3. **Camada de Nuvem (API Backend):** Um servidor RESTful em Java (Spring Boot) que recebe, valida e persiste os logs de voo em um banco de dados relacional para futura plotagem gráfica.

---

## ⚙️ Como Funciona (Snippets de Código)

### 1. Comunicação Ponto a Ponto (ESP-NOW)
Para evitar a latência de redes Wi-Fi tradicionais, utilizamos o protocolo **ESP-NOW**. Os dados trafegam em pacotes binários crus através de uma `struct` C++, garantindo máxima velocidade de amostragem.

```cpp
// Molde do pacote que trafega pelo ar (C++)
typedef struct data_log {
  double temperaturaAmbiente;
  double temperaturaSuperficial;
  unsigned long timestamp; // Tempo de voo em milissegundos
} data_log;
2. Tratamento Assíncrono na Base de Solo
A base recebe o sinal via interrupção de hardware e delega o desenho da tela OLED de forma assíncrona, evitando que o Gateway trave (Kernel Panic) ao receber muitos dados.

C++
// Interrupção ativada instantaneamente quando o pacote de rádio chega
void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {
  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));
  novaLeitura = true; // Levanta a flag segura de forma não-bloqueante
}
3. Persistência na API Spring Boot
Os dados do Gateway são recebidos no Backend através de um Controlador REST, que converte o JSON recebido num Data Transfer Object (DTO) e o envia para o banco de dados via Spring Data JPA.

Java
// Porta de entrada do Servidor (Java)
@PostMapping
public ResponseEntity<TelemetryLog> receberDados(@RequestBody TelemetryRequestDTO dto) {
    TelemetryLog logSalvo = service.salvarTelemetria(dto);
    System.out.println("Pacote salvo: Temp Superficial = " + logSalvo.getTemperaturaSuperficial() + "°C");
    return ResponseEntity.ok(logSalvo);
}
🛠️ Como Testar e Rodar a Aplicação
Requisitos
Java 17+ instalado.

Arduino IDE (com pacotes do ESP32 e bibliotecas Adafruit instaladas).

Postman ou Insomnia (para testar a API sem o hardware).

Passo 1: Subindo a API (Backend)
Navegue até a pasta backend/.

Abra o terminal e rode o comando do Maven para iniciar o servidor:

Bash
./mvnw spring-boot:run
O servidor iniciará na porta 8080 com o banco de dados H2 rodando em memória.

Passo 2: Simulando o Foguete no Postman
Se você não estiver com as placas físicas, pode simular o envio dos dados de telemetria enviando um HTTP POST.

URL: http://localhost:8080/api/telemetry

Método: POST

Headers: Content-Type: application/json

Body (JSON):

JSON
{
  "temperaturaAmbiente": 28.5,
  "temperaturaSuperficial": 85.2,
  "timestampVoo": 12500
}
Nota: Ao enviar este POST, o servidor responderá com o Status 200 OK e o registro será salvo automaticamente com a data e hora do recebimento.

Passo 3: Consultando o Histórico de Voo
Para ver o histórico completo de pacotes (ideal para o frontend desenhar gráficos):

URL: http://localhost:8080/api/telemetry

Método: GET

Passo 4: Rodando o Hardware (ESP32)
Substitua o Endereço MAC (no código do Emissor) pelo MAC da sua placa Receptora.

No código do Receptor, atualize as credenciais do Wi-Fi (ssid e password) e substitua 192.168.X.X pelo IP local da máquina que está rodando o Spring Boot.

Conecte os sensores (Pinos I2C) e faça o upload.

Assim que o foguete for energizado, os dados aparecerão na tela OLED e no console do backend.

💻 Próximos Passos (Roadmap)
[x] Leitura de sensores infravermelhos de alta precisão (GY-906).

[x] Comunicação rádio-frequência ponto a ponto (ESP-NOW).

[x] Alertas visuais operacionais (Display OLED de Solo).

[x] API RESTful Java conectada a banco de dados relacional.

[ ] Desenvolvimento de uma Dashboard Frontend reativa (Vue.js / Vuetify).

MANDE SOMETE A PARTE 1 FORAMTADO EM MD, APOS VC MADNAR SOMENTE A PARTE 1, VOU PEDIR PRA VC MANDAR AS OUTTAS PARETES

Markdown
# 🚀 STGT - Sistema de Telemetria e Gerenciamento Térmico de Baixo Custo

![Badge Status](https://img.shields.io/badge/Status-Em%20Desenvolvimento-green)
![Badge Hardware](https://img.shields.io/badge/Hardware-ESP32%20%7C%20OLED%20%7C%20GY--906-blue)
![Badge Backend](https://img.shields.io/badge/Backend-Java%2017%20%7C%20Spring%20Boot-brightgreen)

## 📌 Sobre o Projeto
O **STGT (Sistema de Telemetria e Gerenciamento Térmico)** é uma solução de engenharia desenvolvida pela Equipe Rocket para resolver o problema do "Voo Cego" em foguetemodelismo. Durante o voo, o atrito aerodinâmico e o calor da propulsão (motor KNSB) podem causar falhas catastróficas na eletrônica embarcada (*Thermal Throttling* e derretimento de trilhas).

Utilizando a filosofia **COTS (Commercial Off-The-Shelf)**, este projeto substitui altímetros e sistemas de telemetria comerciais de alto custo (que ultrapassam R$ 4.500) por uma arquitetura acessível e de alta performance baseada no microcontrolador ESP32.

## 🏗️ Arquitetura do Sistema
O projeto é dividido em três camadas principais:

1. **Camada Embarcada (Foguete):** Lê a temperatura ambiente e superficial via I2C (Sensor GY-906) e transmite os dados estruturados via rádio frequência (2.4 GHz) em tempo real.
2. **Camada de Solo (Receptor):** Capta os pacotes via rádio, exibe alertas visuais críticos numa tela OLED e atua como um *Gateway*, roteando os dados via Wi-Fi para a nuvem.
3. **Camada de Nuvem (API Backend):** Um servidor RESTful em Java (Spring Boot) que recebe, valida e persiste os logs de voo em um banco de dados relacional para futura plotagem gráfica.
VAM

Markdown
---

## ⚙️ Como Funciona (Snippets de Código)

### 1. Comunicação Ponto a Ponto (ESP-NOW)
Para evitar a latência de redes Wi-Fi tradicionais, utilizamos o protocolo **ESP-NOW**. Os dados trafegam em pacotes binários crus através de uma `struct` C++, garantindo máxima velocidade de amostragem.

```cpp
// Molde do pacote que trafega pelo ar (C++)
typedef struct data_log {
  double temperaturaAmbiente;
  double temperaturaSuperficial;
  unsigned long timestamp; // Tempo de voo em milissegundos
} data_log;
2. Tratamento Assíncrono na Base de Solo
A base recebe o sinal via interrupção de hardware e delega o desenho da tela OLED de forma assíncrona, evitando que o Gateway trave (Kernel Panic) ao receber muitos dados.

C++
// Interrupção ativada instantaneamente quando o pacote de rádio chega
void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {
  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));
  novaLeitura = true; // Levanta a flag segura de forma não-bloqueante
}
3. Persistência na API Spring Boot
Os dados do Gateway são recebidos no Backend através de um Controlador REST, que converte o JSON recebido num Data Transfer Object (DTO) e o envia para o banco de dados via Spring Data JPA.

Java
// Porta de entrada do Servidor (Java)
@PostMapping
public ResponseEntity<TelemetryLog> receberDados(@RequestBody TelemetryRequestDTO dto) {
    TelemetryLog logSalvo = service.salvarTelemetria(dto);
    System.out.println("Pacote salvo: Temp Superficial = " + logSalvo.getTemperaturaSuperficial() + "°C");
    return ResponseEntity.ok(logSalvo);
}

Quando estiver pronto para a próxima e última parte (Como Testar e Rodar a Aplicação + Roadmap), é só avisar!
VAMOS FAZER DIFERENTE, SO GERE O ARQUIVO INTEIRO E ME ENIVIE ELE EM .MD pronto pra eu baixar

2. Tratamento Assíncrono na Base de Solo
A base recebe o sinal via interrupção de hardware e delega o desenho da tela OLED de forma assíncrona, evitando que o Gateway trave (Kernel Panic) ao receber muitos dados.

C++
// Interrupção ativada instantaneamente quando o pacote de rádio chega
void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {
  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));
  novaLeitura = true; // Levanta a flag segura de forma não-bloqueante
}
3. Persistência na API Spring Boot
Os dados do Gateway são recebidos no Backend através de um Controlador REST, que converte o JSON recebido num Data Transfer Object (DTO) e o envia para o banco de dados via Spring Data JPA.

Java
// Porta de entrada do Servidor (Java)
@PostMapping
public ResponseEntity<TelemetryLog> receberDados(@RequestBody TelemetryRequestDTO dto) {
    TelemetryLog logSalvo = service.salvarTelemetria(dto);
    System.out.println("Pacote salvo: Temp Superficial = " + logSalvo.getTemperaturaSuperficial() + "°C");
    return ResponseEntity.ok(logSalvo);
}
🛠️ Como Testar e Rodar a Aplicação
Requisitos
Java 17+ instalado.

Arduino IDE (com pacotes do ESP32 e bibliotecas Adafruit instaladas).

Postman ou Insomnia (para testar a API sem o hardware).

Passo 1: Subindo a API (Backend)
Navegue até a pasta backend/.

Abra o terminal e rode o comando do Maven para iniciar o servidor:

Bash
./mvnw spring-boot:run
O servidor iniciará na porta 8080 com o banco de dados H2 rodando em memória.

Passo 2: Simulando o Foguete no Postman
Se você não estiver com as placas físicas, pode simular o envio dos dados de telemetria enviando um HTTP POST.

URL: http://localhost:8080/api/telemetry

Método: POST

Headers: Content-Type: application/json

Body (JSON):

JSON
{
  "temperaturaAmbiente": 28.5,
  "temperaturaSuperficial": 85.2,
  "timestampVoo": 12500
}
Nota: Ao enviar este POST, o servidor responderá com o Status 200 OK e o registro será salvo automaticamente com a data e hora do recebimento.

Passo 3: Consultando o Histórico de Voo
Para ver o histórico completo de pacotes (ideal para o frontend desenhar gráficos):

URL: http://localhost:8080/api/telemetry

Método: GET

Passo 4: Rodando o Hardware (ESP32)
Substitua o Endereço MAC (no código do Emissor) pelo MAC da sua placa Receptora.

No código do Receptor, atualize as credenciais do Wi-Fi (ssid e password) e substitua 192.168.X.X pelo IP local da máquina que está rodando o Spring Boot.

Conecte os sensores (Pinos I2C) e faça o upload.

Assim que o foguete for energizado, os dados aparecerão na tela OLED e no console do backend.

💻 Próximos Passos (Roadmap)
[x] Leitura de sensores infravermelhos de alta precisão (GY-906).

[x] Comunicação rádio-frequência ponto a ponto (ESP-NOW).

[x] Alertas visuais operacionais (Display OLED de Solo).

[x] API RESTful Java conectada a banco de dados relacional.

[ ] Desenvolvimento de uma Dashboard Frontend reativa (Vue.js / Vuetify).
"""

with open("README.md", "w", encoding="utf-8") as file:
file.write(markdown_content)

Seu arquivo Markdown (.md) está pronto para download.
[file-tag: code-generated-file-0-1782073729503258464]

Pode guardar este arquivo diretamente na raiz do seu repositório `Sistema-de-Telemetria-de-Baixo-Custo`. Ele inclui a documentação completa, badges informativos, todos os snippets essenciais que abordámos e as instruções de teste do Postman perfeitamente formatadas. Quando for visualizado no GitHub, terá um aspeto de projeto profissional finalizado!
vc mandou tudo quebrado, fodase, os topicos tao quebrando oi MD

O seu arquivo Markdown (.md) está pronto
Ícone do app MD
README-v2
 MD 
Aberto

Peço desculpa pelo problema com o texto na mensagem anterior. Dependendo de onde se copia o texto (especialmente no GitHub), blocos de código podem "quebrar" se os espaçamentos das linhas em branco (\n\n) não forem lidos corretamente pelo navegador.

Neste novo arquivo README-v2.md gerado agora, inseri quebras de linha absolutas por código, garantindo que os blocos de código (cpp, java, json) e os títulos se separam perfeitamente.

Basta fazer o download deste ficheiro e substituí-lo na pasta raiz do seu projeto. Pode renomeá-lo simplesmente para README.md antes de fazer o commit!




O Gemini é uma IA e pode cometer erros.

# 🚀 STGT - Sistema de Telemetria e Gerenciamento Térmico de Baixo Custo

![Badge Status](https://img.shields.io/badge/Status-Em%20Desenvolvimento-green)
![Badge Hardware](https://img.shields.io/badge/Hardware-ESP32%20%7C%20OLED%20%7C%20GY--906-blue)
![Badge Backend](https://img.shields.io/badge/Backend-Java%2017%20%7C%20Spring%20Boot-brightgreen)

## 📌 Sobre o Projeto

O **STGT (Sistema de Telemetria e Gerenciamento Térmico)** é uma solução de engenharia desenvolvida pela Equipe Rocket para resolver o problema do "Voo Cego" em foguetemodelismo. Durante o voo, o atrito aerodinâmico e o calor da propulsão (motor KNSB) podem causar falhas catastróficas na eletrônica embarcada (*Thermal Throttling* e derretimento de trilhas).

Utilizando a filosofia **COTS (Commercial Off-The-Shelf)**, este projeto substitui altímetros e sistemas de telemetria comerciais de alto custo por uma arquitetura acessível e de alta performance baseada no microcontrolador ESP32.

---

## 🏗️ Arquitetura do Sistema

O projeto é dividido em três camadas principais:

1. **Camada Embarcada (Foguete):** Lê a temperatura ambiente e superficial via I2C (Sensor GY-906) e transmite os dados estruturados via rádio frequência (2.4 GHz) em tempo real.
2. **Camada de Solo (Receptor):** Capta os pacotes via rádio, exibe alertas visuais críticos numa tela OLED e atua como um *Gateway*, roteando os dados via Wi-Fi para a nuvem.
3. **Camada de Nuvem (API Backend):** Um servidor RESTful em Java (Spring Boot) que recebe, valida e persiste os logs de voo em um banco de dados relacional para futura plotagem gráfica.

---

## ⚙️ Como Funciona (Snippets de Código)

### 1. Comunicação Ponto a Ponto (ESP-NOW)

Para evitar a latência de redes Wi-Fi tradicionais, utilizamos o protocolo **ESP-NOW**. Os dados trafegam em pacotes binários crus através de uma `struct` C++, garantindo máxima velocidade de amostragem.

```cpp
// Molde do pacote que trafega pelo ar (C++)
typedef struct data_log {
  double temperaturaAmbiente;
  double temperaturaSuperficial;
  unsigned long timestamp; // Tempo de voo em milissegundos
} data_log;
```

### 2. Tratamento Assíncrono na Base de Solo

A base recebe o sinal via interrupção de hardware e delega o desenho da tela OLED de forma assíncrona, evitando que o *Gateway* trave (*Kernel Panic*) ao receber muitos dados.

```cpp
// Interrupção ativada instantaneamente quando o pacote de rádio chega
void OnDataRecv(const uint8_t * mac, const uint8_t *incomingData, int len) {
  memcpy(&payloadRecebido, incomingData, sizeof(payloadRecebido));
  novaLeitura = true; // Levanta a flag segura de forma não-bloqueante
}
```

### 3. Persistência na API Spring Boot

Os dados do Gateway são recebidos no Backend através de um Controlador REST, que converte o JSON recebido num Data Transfer Object (DTO) e o envia para o banco de dados via Spring Data JPA.

```java
// Porta de entrada do Servidor (Java)
@PostMapping
public ResponseEntity<TelemetryLog> receberDados(@RequestBody TelemetryRequestDTO dto) {
    TelemetryLog logSalvo = service.salvarTelemetria(dto);
    System.out.println("Pacote salvo: Temp Superficial = " + logSalvo.getTemperaturaSuperficial() + "°C");
    return ResponseEntity.ok(logSalvo);
}
```

---

## 🛠️ Como Testar e Rodar a Aplicação

### Requisitos

* Java 17+ instalado.
* Arduino IDE (com pacotes do ESP32 e bibliotecas Adafruit instaladas).
* Postman ou Insomnia (para testar a API sem o hardware).

### Passo 1: Subindo a API (Backend)

1. Navegue até a pasta `backend/`.
2. Abra o terminal e rode o comando do Maven para iniciar o servidor:
   ```bash
   ./mvnw spring-boot:run
   ```
3. O servidor iniciará na porta `8080` com o banco de dados H2 rodando em memória.

### Passo 2: Simulando o Foguete no Postman

Se você não estiver com as placas físicas, pode simular o envio dos dados de telemetria enviando um **HTTP POST**.

* **URL:** `http://localhost:8080/api/telemetry`
* **Método:** `POST`
* **Headers:** `Content-Type: application/json`
* **Body (JSON):**
```json
{
  "temperaturaAmbiente": 28.5,
  "temperaturaSuperficial": 85.2,
  "timestampVoo": 12500
}
```
> **Nota:** Ao enviar este POST, o servidor responderá com o `Status 200 OK` e o registro será salvo automaticamente com a data e hora do recebimento.

### Passo 3: Consultando o Histórico de Voo

Para ver o histórico completo de pacotes (ideal para o frontend desenhar gráficos):

* **URL:** `http://localhost:8080/api/telemetry`
* **Método:** `GET`

### Passo 4: Rodando o Hardware (ESP32)

1. Substitua o Endereço MAC (no código do Emissor) pelo MAC da sua placa Receptora.
2. No código do Receptor, atualize as credenciais do Wi-Fi (`ssid` e `password`) e substitua `192.168.X.X` pelo IP local da máquina que está rodando o Spring Boot.
3. Conecte os sensores (Pinos I2C) e faça o upload.
4. Assim que o foguete for energizado, os dados aparecerão na tela OLED e no console do backend.

---
